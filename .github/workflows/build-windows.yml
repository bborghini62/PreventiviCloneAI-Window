name: Build Windows Setup Beta 6.3.0

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Install dependencies
        run: npm install

      - name: Prepare PDF.js
        run: npm run prepare:pdfjs

      - name: Verify JavaScript syntax
        run: npm run verify:syntax

      - name: Verify application buttons
        run: npm run verify:buttons

      - name: Verify independent calculations
        run: npm run verify:calc

      - name: Verify no redraw while editing
        run: npm run verify:cursor

      - name: Build Windows installer
        run: npm run tauri build -- --bundles nsis

      - name: Upload Windows installer
        uses: actions/upload-artifact@v4
        with:
          name: PreventiviCloneAI-Windows-Setup-Beta-6.3.0
          path: src-tauri/target/release/bundle/nsis/*.exe
          if-no-files-found: error
